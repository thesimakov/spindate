node_modules

# Секреты и локальные env (на VPS — только вне репозитория)
.env
.env.local
.env.*.local

.next
.DS_Store
.git_backup
.api-backup
.pnpm-store
data
out

# TypeScript build info (локальный кэш)
tsconfig.tsbuildinfo
*.tsbuildinfo
