# Пример фрагмента NGINX для Spindate (не копировать слепо — подставить порты и домены).
# Полная инструкция: DEPLOY_FROM_SCRATCH.md, обзор: SCALING.md
#
# limit_req_zone — грубый лимит по IP на origin (дополняет Cloudflare и Redis rate-limit в приложении).

# http { ... }
# limit_req_zone $binary_remote_addr zone=spindate_api:10m rate=30r/s;
# limit_req_zone $binary_remote_addr zone=spindate_ws:10m rate=50r/s;
# upstream next_app {
#     least_conn;
#     server 127.0.0.1:3002;
#     # server 127.0.0.1:3003;
# }
# # Sticky для WebSocket: отдельный upstream с ip_hash (или hash $arg_room consistent;)
# upstream ws_app {
#     ip_hash;
#     server 127.0.0.1:3002;
#     # server 127.0.0.1:3004;
# }

# server {
#     listen 443 ssl http2;
#     server_name example.com;
#
#     location /ws/ {
#         limit_req zone=spindate_ws burst=100 nodelay;
#         proxy_pass http://ws_app;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 3600s;
#         proxy_send_timeout 3600s;
#     }
#
#     location /api/ {
#         limit_req zone=spindate_api burst=80 nodelay;
#         proxy_pass http://next_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         limit_req zone=spindate_api burst=80 nodelay;
#         proxy_pass http://next_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
